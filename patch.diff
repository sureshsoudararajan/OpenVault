--- apps/api/prisma/schema.prisma
+++ apps/api/prisma/schema.prisma
@@ -51,6 +51,7 @@
   recoveryCodes       RecoveryCode[]
   tags                Tag[]
   ytdlpCookies        YtdlpCookie[]
+  fileRequestLinks    FileRequestLink[]
 
   @@map("users")
 }
@@ -165,6 +166,7 @@
   files       File[]
   permissions Permission[] @relation("FolderPermissions")
   shareLinks  ShareLink[]  @relation("FolderShareLinks")
+  fileRequestLinks FileRequestLink[]
 
   @@index([userId])
   @@index([parentId])
@@ -359,3 +361,25 @@
   @@unique([userId, name])
   @@map("ytdlp_cookies")
 }
+
+// ============================================
+// File Requests (Link Drops)
+// ============================================
+
+model FileRequestLink {
+  id          String    @id @default(uuid()) @db.Uuid
+  folderId    String    @map("folder_id") @db.Uuid
+  token       String    @unique @db.VarChar(64)
+  title       String    @db.VarChar(255)
+  description String?   @db.Text
+  expiresAt   DateTime? @map("expires_at")
+  isActive    Boolean   @default(true) @map("is_active")
+  createdById String    @map("created_by") @db.Uuid
+  createdAt   DateTime  @default(now()) @map("created_at")
+  updatedAt   DateTime  @updatedAt @map("updated_at")
+
+  folder    Folder @relation(fields: [folderId], references: [id], onDelete: Cascade)
+  createdBy User   @relation(fields: [createdById], references: [id], onDelete: Cascade)
+
+  @@index([token])
+  @@index([folderId])
+  @@map("file_request_links")
+}
